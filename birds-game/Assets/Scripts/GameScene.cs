namespace birds_game.Assets.Scripts
{
    /// <summary>
    /// ATTENTION! This has to be in sync with scene build index ( Build Settings )
    /// </summary>
    public enum GameScene
    {
        Intro = 0,
        Beach = 1,
        City = 2,
        Arctic = 3,
        Outro = 4,
        ScoreBoard = 5
    }
}